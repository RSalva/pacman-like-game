// board config
const tileSize = 25;
const rows = 28;
const columns = 31;
const boardWidth = rows * tileSize;
const boardHeight = columns * tileSize;
const gameMargin = 1;


// game config
const fps = 60;
const movementSpeed = 1;

// pacman config
const pacmanInitX = 25 * tileSize
const pacmanInitY = 29 * tileSize;
const pacmanWidth = tileSize;
const pacmanHeight = tileSize;

// ghosts config
const ghost1InitX = 1 * tileSize;
const ghost1InitY = 1 * tileSize;
const ghost1Width = tileSize;
const ghost1Height = tileSize;
